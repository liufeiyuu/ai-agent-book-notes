# 带学路线：从一笔订单走完真实 RAG

原则：代码由助手搭好；学习者负责预测、追踪、改动、解释和复原。每次只学一站，先完成当前问题，再看下一站，不要求通读整个项目。

进度：实现和离线检查已完成；真实 API 配置、真实运行与学习验收待完成。

## 第一站：先看一次固定 RAG，不展开 Agent Loop

只打开 `src/runner.ts`，找到 `runConsultation` 中 `if (options.mode === "rag")` 这段。

这段不是让你记语法，而是看四种东西的转换：

1. `visibleOrders(...)` 拿到业务事实，不是让模型猜订单。
2. `retrieve(...)` 根据订单范围检索政策，返回 `search.hits`。
3. `buildContext(...)` 把问题、订单、证据整理成 messages。
4. `model.generate(...)` 把这些 messages 交给真实模型。

先不要跳进每个函数。看清它们的输入/输出，回答三个问题：

1. `retrieve` 得到的结果，保存在哪个变量里，之后传给谁？请指出具体代码。
2. `model.generate` 有没有接收整个知识库？你从哪一行判断？
3. 如果删掉 `buildContext` 的检索证据，仅保留问题和订单，模型还可能答对吗？即使答对，能证明什么、不能证明什么？

过关标准：能用自己的话串起来“查询事实 → 取得证据 → 构建请求 → 模型生成”。这次不要求你读完 provider、缓存或循环实现。

## 第二站：跑一次真实请求，逐字段对照

本地配置 `.env` 后，先 `npm run index`，再 `npm run ask -- --case normal --mode rag`。遇到错误就看错误，不使用测试替身生成“成功”结果。

先读政策原文和 H1001 订单，写下预测。然后在输出 Trace 中只看：

1. `searches[0].scope`：版本选择依据来自哪里？
2. `searches[0].hits`：证据块 ID、原文、名次是什么？
3. `requests[0].messages`：这些证据确实出现在真实 API 请求里吗？
4. `rawAnswer` 和 `evaluation.checks`：模型怎样使用证据，哪项通过/失败？

回答 Trace 中以上字段位于 `cases[0]` 下。不要先看巨大向量数组，也不要只报“全部绿了”。

要交付的一句话：模型因为读到了 **哪个块的哪条条件**，结合 **哪项订单事实**，所以给出了 **什么结论**。若答错，也按这条链定位断点。

## 第三站：弄清索引和检索

看 `corpus.ts` 的 `chunkDocument`、`provider.ts` 的 `embed`、`index-store.ts` 的 `buildIndex`、`retrieval.ts` 的 `retrieve`。

只抓五件事：完整条件不能在分块时消失；每块带来源/适用时间；文档和查询必须使用同一 Embedding 模型；过滤不是语义打分；Top-K 只选择结果，不会重算分数。

实践任务：同一个查询、订单和模型，分别 `--top-k 1` 与 `--top-k 3`，用 `npm run retrieve` 运行。比较排除原因、完整 ranking、最终 hits。若关键块本来就排第一，不要硬说扩大 K 改善了召回；记录“本查询没有获得额外关键证据”也是收获。

补充检查：修改一小段政策正文后，先执行 retrieve，观察旧索引被拒绝。然后重建索引再试，最后恢复改动并再次重建。不要靠删除旧版本政策解决日期选择问题。

## 第四站：回到真正的 Agent

先预测 `normal` 与 `ambiguous` 两个案例的工具调用应该有什么不同，再执行各自的 `--mode agent`。

只看 `tools.ts` 和第一周 `agent.ts`：模型输出工具名称/参数 → Harness 校验与执行 → 工具结果带相应调用 ID 进入 messages → 模型下一轮决策。

去 Trace 找实际的 `query_orders`、`search_policy`、下一轮 messages。不要假定一定按理想流程执行；如果模型跳过检索，检查如何揭示了这个问题？如果无限重复，谁负责停止？

过关标准：能用真实 Trace 说明“这里是模型决策，那里是程序执行”，而不只复述 ReAct。

## 第五站：亲手改一处业务 Prompt

在阅读 `context.ts` 后，先独立写出“证据不足时应该怎样回答”的规则，再与现有规则对照。选一个观察到的真实失败，只改一条相关规则，保留改前/改后 Trace。

控制变量：同一个案例、同一个聊天模型、同一份索引、相同 Top-K。说明为什么你认为问题发生在生成或工具决策阶段，而不是召回阶段。

若基线没有失败，可以新增一个有明确标准答案的小案例（例如用户问错渠道），观察边界；不要为了产生失败随意删安全规则。小样本有随机性，temperature=0 也不是绝对确定，不得把一次变化说成稳定提升。

过关标准：你能解释自己改动的机制和证据，不是“照着建议改完测试绿了”。恢复实验性改动，或明确记录为什么保留。

## 第六站：验收和结论

读 `evaluation.ts`，运行六个案例。记录这张表，不预填结果：

| 案例 | 预测 | 关键块进入上下文？ | 工具流程合理？ | 状态/引用检查 | 人工核对结论 | 失败层 |
| --- | --- | --- | --- | --- | --- | --- |
| normal | 待填 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 |
| paraphrase | 待填 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 |
| historical | 待填 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 |
| missing | 待填 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 |
| ambiguous | 待填 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 |
| exception | 待填 | 待运行 | 待运行 | 待运行 | 待运行 | 待运行 |

最后用“假设、变量、观察、解释、限制”总结一次受控改动。这个项目真正的收获应是：你能够沿实际输入/输出诊断一个真实模型系统，而不只是知道 RAG 的名词。

## 暂时不用学

`cli.ts` 参数解析、`saveJson` 原子写入细节、HTTP 辅助实现、所有测试代码、余弦数学推导。遇到对应问题再回查；它们不是第一轮阅读入口。
