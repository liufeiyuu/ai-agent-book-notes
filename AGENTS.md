# 仓库协作指引

## 学习计划与接续上下文统一维护在仓库

2026-09-14 用户明确约定：后续学习计划、进度、关键决定和接续上下文只在本代码仓库维护，停止维护 Notion 同步副本。

- 本仓库 Markdown 是唯一维护版本。Notion 旧页面和现有链接仅作历史来源；不自动创建、更新或同步学习页面，也不依赖读取 Notion 恢复当前进度。用户后续明确提出的新要求优先。
- 每个阶段结束或暂停时，在对应计划的“执行进度”记录完成证据、学习者与助手各自完成的工作、待解决问题和唯一下一步；记录该实验实际调用等适用信息，不虚记完成状态。
- 用户调整目标、范围或学习方式时，写回对应计划；长期方向写入能力路线，通用协作规则写入本文件，延后项统一进入[延后学习清单](notes/learning-backlog.md)。详细进度只维护在对应阶段计划，其他位置通过链接引用。
- 保存继续学习所需的要点和证据路径，不要求复制整段聊天。仅要求整理或修改计划时，不据此开始教学、实验或模型调用。

## 每次带学前完整读计划，按“抓大放小”执行

2026-09-14 用户明确要求：把抓大放小的执行边界保存在计划中，并通过本文件要求模型每次带学前重新阅读，防止偏离主线。

1. **每次开始或继续带学前，包括同一会话的下一次带学回复，必须先从仓库完整读取当前计划。** 为该课程准备实验实现或演示前也执行。先从 [README 的“当前学习计划”](README.md#当前学习计划)定位计划，再完整读所指计划正文与“执行进度”，重点核对范围、实现深度、预计用时、最小实践和结束标准。不能以聊天摘要、以前读过、只读标题或进度、关键词检索片段代替全文读取；新会话及上下文压缩后同样执行。[全栈能力路线](notes/agent-fullstack-roadmap.md)用于查阅阶段依赖，不作为当前阶段的另一份记录。
2. **当前计划只由 README“当前学习计划”中的“当前计划”链接指定。** 本文件维护稳定带学规则，各阶段计划维护内容和实际进度，不在本文件或路线里重复指定当前第几周。阶段切换时先在原计划写回进度，再更新 README 的当前计划链接，并完整读新计划；无需因阶段切换同步多份入口。用户明确指定开始或恢复另一课程时，按新要求更新入口并读取该课程自己的计划。
3. 带学开头用一两句话说明已读取的计划、当前学习块和本次目标，从“执行进度”的下一步接续。已通过的内容直接引用证据，不重新从头教学；计划仍为未开始时，不声称已实现或通过。
4. **默认由助手实现、调试和验证，学习者重点理解行为、结果与设计取舍，达到计划验收标准就收尾。** 2026-09-14 用户明确不希望以亲手写代码作为学习任务；除非用户主动选择，不再布置手写函数、补代码或誊写规格作为门槛。真实运行与必要验证保留，助手代写、代跑与学习者操作分别记录，不能把实现通过自动记为学习者已理解。按计划复用代码与 Trace，完整带读选定实验的代码与测试，串清入口、调用关系、正常与失败路径；“抓大放小”用于划定学习范围，不能用来省略选定实验尚未读完的部分。不自动扩展到无关依赖库源码、完整框架建设或额外考试。各阶段的具体范围只在各自计划中维护。
5. 新增讲解、实现或检查前，核对它是否直接支持当前验收。阻塞主线的问题只补足必要部分；非阻塞扩展记入[延后学习清单](notes/learning-backlog.md)，后端工程能力按路线接续，不临时加为本单元门槛。用户后续明确调整范围或学习方式时，写回计划后按新约定推进。
6. 暂停或结束时写回对应计划的证据、实际投入、缺口和唯一下一步。预计时间与实际用时分开，无法核实的时间标为估算；各课程的预算仅按各自计划执行，不相互套用。只修改或讨论计划不触发带学、实验或模型调用。

## 固定学习顺序：先原始资料，再实验代码

2026-09-17 用户明确认可并要求保存以下规则，替代此前原文和代码逐小段交替推进的默认安排：

1. **先带读本章原始资料。** 核对来源，划清重点、略读与暂时跳过的范围，分小段解释核心概念，建立整体理解；不把全文丢给学习者独自完成。
2. **本章约定的原文阅读完成后，再带读对应实验代码。** 先明确是哪套实验及文件范围，从入口开始，沿调用关系完整读实现、输入输出、错误处理与测试。讲代码时回扣已读原文，不重新布置已读原文，也不只给概述就跳过未读代码。
3. **原文阅读和对应实验代码学习两部分都完成，并具备计划要求的运行证据后，再进入下一章。** 以学习者实际进度为准，助手写完代码、讲解过或测试通过均不能代替学习者学完。
4. **“抓大放小”用于选择范围，选定要学的实验就完整讲清楚。** 不以省时间为由遗漏选定代码，也不因带读发现扩展话题就新增实验。默认助手写代码，学习者理解与判断，不布置手写代码门槛。
5. 当前计划分别维护原文阅读、学习者代码带读、助手实现与运行验证三个维度，并记录代码阅读停点及唯一下一步。用户明确要求重新从头读时按新要求执行，历史证据保留，不能把新一轮阅读预先标为完成。

本规则适用于按章节学习原书及对应实验；独立补充课仍按用户为该课约定的计划执行。用户后续明确调整顺序时，先更新对应计划再按新约定推进。只要求修改规则时，不自动开始或继续教学。

## 原书带读与实验分别记录

2026-09-16 用户明确：原文独自阅读费劲，需要助手对照章节带读。涉及原书学习时，先核对原文标题与段落，为当前计划提供必读、略读、延后地图；每轮给明确起止位置、小段解释和已有实验对应关系，不用自建实验与答题替代原文。不以“实验通过”推定“章节读完”。作者观点、助手建议和实际运行结论分别说明；章节阅读缺口及暂停位置写回当前计划。用户没有要求时，不把延后内容扩成新的实验或考试。

2026-09-17 用户再次纠正：**原文阅读、助手实现/运行验证、学习者实验与代码带读必须分开记录，三者不能互相替代。** 用户说“本章读完”时，只更新其明确所指的阅读维度；如已有代码带读缺口，继续原实验，不据此推进下一章。代码带读须在当前计划记录实际文件、函数/段落、学习者反馈及未解问题；助手讲过、生成过代码或测试通过不能自动记为学习者学完。历史停点无证据时明确标未知，依据最后可核对主题接续，不虚构停行、不让用户重读已确认理解的内容。详细清单只在当前计划维护。

## 用户记忆两小时补充学习：防止教学范围漂移

以下规则只适用于本仓库中“从聊天提取记忆 → 保存 → 新对话读取 → 用户纠错后更新”的补充实验及带学。其他任务按用户当次请求处理，不自动套用本课预算，也不把话题强行拉回本课。

### 继续前先恢复计划与进度

- 每次开始或继续本课的实现、演示或教学前，完整阅读 [两小时学习计划](notes/03-memory-and-knowledge/user-memory-two-hour-plan.md)，核对文末“执行进度”。新会话、上下文压缩后也执行此步骤，不只依赖聊天摘要。
- 以计划作为本课范围、时间安排和验收标准的依据。第二周核心验收已经完成，本课是主动补充，不重新打开第二周必修清单。
- 当用户只是要求保存、修改或讨论计划时，不据此开始实验、调用模型或运行教学计时；等待用户明确要求开始或继续执行。

### 固定范围与累计预算

- 只做虚构用户的“代码讲解详略”这一个偏好字段：真实模型提取候选、程序校验、本地 JSON 保存、按用户和字段读取、明确纠错更新、基本用户隔离。
- 不增加向量数据库、复杂框架、自动遗忘、生产部署或通用记忆治理。不以“顺便学一下”为由增加必学模块或结业门槛。
- 总安排为 110 分钟内容加最多 10 分钟缓冲，包含助手准备、调试和跟学。真实聊天接口调用总上限为 9 次（用户已批准在原 8 次外增加 1 次助手复验），准备与失败尝试也计入；讲解复用已有真实 Trace，保留学习者的 4 次名额。
- 时间、接口次数跨阶段和会话累计，不因新命令、新进程或新对话重置。接口故障最多占用计划中的 10 分钟缓冲；到限说明完成项和缺口，未经用户明确同意不加时、不加次数、不换模型。
- 用户明确要求改变范围或预算时，先说明对时间的影响并同步更新计划；文件中的旧约定不能覆盖用户后续的明确调整。

### 带学方式与结束标准

- 每个教学回合先简短说明当前阶段、本次目标及剩余预算；无法精确核实用时就标注估算，不伪造计时。
- 只带读提取、校验保存、读取注入和更新的关键代码，提供实际文件位置与输入输出，不逐行展开无关语法或辅助实现。
- 学习者卡住时，直接用实际数据解释，不连续用模糊的是非题拖长课程。非阻塞扩展记入延后事项，除非用户明确选择，否则继续原主线。
- 分别验证：记忆已保存、进入新进程的真实模型请求、回答实际遵循偏好；不能以其中一项代替其余项。纠错后旧值不再当作当前偏好使用，并验证用户隔离。
- 严格区分助手实现、学习者操作、真实模型结果与离线演示。未运行的阶段不能标为完成；离线测试通过不能冒充真实闭环通过。
- 计划约定的四项验收满足即收尾，不临时追加考试、实验或全量源码阅读。

### 写回进度，方便下一次接续

- 每个阶段结束或暂停时，将状态、已完成证据、累计用时、累计接口尝试、待验证缺口和唯一下一步写回计划的“执行进度”。
- 用可核对的开始/结束时间累计本课实际工作与教学时段；明确的休息和暂停不计入。无法确认的时段标记估算或待确认，不能重置为零。
- 进度仍为“尚未开始”时，保持实现、运行和验收均未完成；恢复时从最后一个有证据的检查点继续，不重新讲完全部内容。

本文件是项目级指引，不是平台系统提示词，也不替代更高优先级的指令或权限检查。完整计划保留在笔记中，本文件负责要求按需读取和执行。
