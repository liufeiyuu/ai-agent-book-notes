---
title: 第四周学习计划：第 6–7 章异步 Agent、交互与评估
chapters: [6, 7]
type: plan
status: planned
updated: 2026-09-14
---

# 第四周学习计划：第 6–7 章异步 Agent、交互与评估

> 状态：已从第三周拆出为独立学习单元，尚未开始。
>
> 先完成[第三周第 4–5 章](./week-03-plan.md)，再进入本单元。继续按内容与交付物推进，提供预计用时，不设完成日期、每周投入或固定课时上限。

## 范围与顺序

先学第 6 章的**异步 Agent（含 `async-agent` 最小实践）**及相关交互机制，再学第 7 章的系统评估。目标是：**追踪后台任务、用户输入与完成事件怎样共同驱动 Agent，理解等待、继续、失败和取消，再用明确标准评价结果与过程。**

旧 Notion 的“工具、MCP 与异步 Agent”拆为两个学习单元：工具/MCP 进入第三周，异步 Agent 进入第四周。[旧双月 OKR](https://app.notion.com/p/3be0d28175e780b88d22d8ddc42945f3)中的“运行并理解 `async-agent`”保留为明确实践；之前拆分版仅列状态与错误控制，遗漏了异步事件链路的运行要求，本次补齐。

第三周只需要完成工具、MCP 和一次 Coding 修改。它的必要测试、错误反馈与单次运行限制仍保留；本页的状态机、组合故障案例和系统评测不作为第三周结业门槛。

自建机制实验继续使用 TypeScript，复用第三周拟建的 `experiments/tool-runtime-lab/`、已有 Loop 与退款项目证据；这些新增内容均未实现。官方 `async-agent` 是 Python 项目，可由助手准备最小运行入口并带读数据流，不要求学习者先完成 Python 课程，也不要求同时实现两种语言的版本。Python 系统学习仍列在这两组之后，见[全栈能力路线](./agent-fullstack-roadmap.md)。

## 每块预计学习时间

以下将 A、B 拆成便于安排的学习块，内容与验收不变。**每块包含定向阅读、带学讲解、学习者参与行为核对、一般改错、核对结果与解释**。估算以第三周已完成、复用已有实验与 Trace、助手提前跑通选定的最小入口为前提。

| 学习块 | 预计学习时间 | 对应内容与主要活动 |
| --- | --- | --- |
| A1. 交互与异步框架 | 1–1.5 小时 | 概览交互形式；区分顺序等待与事件驱动；理解任务 ID、事件队列、Loop 续接与状态转移 |
| A2. 异步 Agent 最小实践 | 3–4 小时 | 带读运行入口；亲跑任务启动、中途输入、完成事件与后续处理；改变一次输入或取消时机，整理事件记录 |
| A3. 执行边界 | 2–3 小时 | 观察审批、重复提交、取消与有界重试；讨论一处关键判断修改，由助手实现；核对结果并解释状态与失败层次 |
| B1. 评估方法 | 1–1.5 小时 | 理清任务、样本、预期与判定规则；区分工具测试、任务评测、过程检查及人工/Judge 的职责 |
| B2. 评测与归因实践 | 2–3 小时 | 整理已有案例；补一条检查或边界；对照成功/失败 Trace，解释已有指标，写一条评分标准 |
| 最终整理 | 0.5–1 小时 | 汇总异步链路与评测证据、补齐笔记链接、写回进度和下一步 |
| **核心学习合计** | **9.5–14 小时** | **其中第 6 章 A1–A3 为 6–8.5 小时，第 7 章 B1–B2 为 3–4.5 小时；另含最终整理** |

另列助手准备和额外排障，方便后续安排：

| 项目 | 预计时间 | 计时口径 |
| --- | --- | --- |
| 助手独立准备 | 2–4 小时 | 跑通选定异步实验入口、准备边界演示、选取可复用 Trace 与记录模板；不计入你的核心学习时间。同步带学已计入上表 |
| 额外排障预留 | 2–3 小时 | 应对超出一般改错的环境问题或反复卡住的事件顺序、状态理解；按实际发生记录，不默认用完。只有你参与的部分增加你的学习投入 |

**最容易超出估算的是 A2。** 3–4 小时以助手已准备好最小运行路径、只带读关键事件与任务数据流为前提；不要求学习者同时补完 Python 或重写事件循环，也不同时实现 Python、TypeScript 两个版本。若实际入口或理解难度超出前提，先根据卡点调整估算。

A2 已完成的取消或补充输入时机对照，在 A3 直接引用；B2 复用前面已有案例和 Trace，不重做所有实验。各块已含现场验收，最终整理只归档和总览。准备与学习并行时不能直接相加当作实际历时，同一段排障也不重复计入准备和缓冲。

这些区间是安排节奏的参考，**不是实际用时记录，也不是硬性上限或新期限**；开始后按实际进展修正。

## 抓大放小：带学执行边界

按[仓库协作指引](../AGENTS.md)，每次开始或继续本单元带学前，须完整阅读本计划并核对文末“执行进度”；新会话、上下文压缩后同样执行，不能只凭聊天摘要恢复。

- **异步核心机制必须亲跑**：学习者追踪“任务启动 → 中途用户事件 → 完成/取消 → 后续处理”，核对任务 ID、事件顺序和结果回传，并完成已有的输入时机对照。状态图、助手口述和单独的 `async/await` 示例不能代替这条完整事件链。
- **A3 共用实验，保留代表场景**：围绕同一虚构草稿操作，覆盖原表的未批准/批准/拒绝、同键重复与参数冲突、写入前取消、暂时失败与不可重试失败。助手完成审批或幂等判断中的一处修改；学习者核对修改前后行为并理解原因，不要求手写。A2 已有的取消证据直接引用，不把四类边界扩成四套完整系统。
- **阅读与实现限定在最小路径**：助手准备可运行入口，带读聚焦事件、任务和调度的关键代码；不补完整 Python 课程，不通读官方框架，也不要求同时实现 Python、TypeScript 两个版本。
- **评测复用已有证据**：保留 B 的评测表、一条检查或边界补充、成功/失败归因与简短评分标准；不新建评测平台、不接入新的 Judge 模型，也不为补指标默认重跑接口。确需新的模型验证时，先明确它要补的证据缺口，并区分真实结果与离线机制演示。
- **达到已有两项验收即收尾**：必要运行、关键判断修改和验证不可省略；不追加考试、全量案例重跑或生产恢复实现。非阻塞细节写入[延后学习清单](./learning-backlog.md)，数据库、Worker、跨进程恢复等工程能力按[全栈能力路线](./agent-fullstack-roadmap.md)接续。
- 学习者后续明确调整范围时，以新要求为准，先同步本计划的范围、预计用时与验收，再按新约定执行；不把助手准备或排障中的扩展变成新的学习门槛。

2026-09-14 按用户最新要求，代码默认由助手实现，学习者聚焦行为、结果与设计取舍；取消手写门槛，保留真实异步链路和必要验证。原用时区间暂作参考，代码投入转由助手承担，后续按实际带学耗时调整。

## A. 第 6 章：异步 Agent 与运行控制

先概览本章的两个维度：观察/动作的形式，以及交互的触发时机。语音、Computer Use 和机器人只用于认识不同交互场景，本单元实践集中在异步任务与运行控制。

**需要掌握：**

- 从一次调用中顺序等待工具，到后台任务与事件驱动的 Agent，运行方式增加了什么；仅使用 `async/await` 不能证明整个 Agent 可以响应外部事件。
- 启动长任务后如何立即取得任务 ID，任务尚未完成时怎样接收用户查询、补充要求或取消指令。
- 事件队列/分派器怎样处理用户输入与任务完成事件，怎样按任务 ID 关联结果，并在安全的处理位置更新轨迹、续接 Loop；接收输入不等于任意时刻并发修改模型消息。
- 一次工具调用的状态、一个 Agent Run 的状态和业务完成状态如何区分。
- `pending`、`running`、`waiting_approval`、`succeeded`、`failed`、`cancelled` 的含义与允许的转移。
- 权限与人工审批如何控制执行；批准的操作和参数需要怎样对应。
- 超时、重试、幂等分别解决什么问题；哪些失败适合重试，为什么需要上限。
- 取消怎样传递、为什么不等于撤销已有效果；恢复还需要保存哪些状态。Trace 记录不自动提供恢复能力。

### 核心实践：运行并理解 `async-agent`

阅读入口为[官方 `chapter6/async-agent`（实验 6-2）](https://github.com/bojieli/ai-agent-book/tree/main/chapter6/async-agent)。先看 README、事件/任务模型与调度入口，运行选定的离线最小路径；保留其与真实模型场景的区别。若采用 TypeScript 等价实现，记录为机制复现，不能记为已经运行了官方 Python 项目。

学习者亲自运行并追踪以下过程；可用受控任务与脚本化模型观察机制，不要求新增付费模型调用：

1. 启动一个尚未完成的任务，获得任务 ID 与运行状态。
2. 在它完成前发送查询或补充要求，确认输入能被接收；说明它被立即处理还是排队等待。
3. 任务完成后，按同一任务 ID 收到完成事件；结果与适用的用户更新进入下一轮处理，输出最终结果。
4. 改变一次取消或补充输入的时机，观察处理顺序、任务状态和最终结果的变化。

证据至少能串起“任务启动 → 中途用户事件 → 完成/取消 → 后续处理”，包含事件顺序、任务 ID、状态和结果回传。仅画状态图或给函数加 `async` 不算此项通过。离线验证证明运行机制；没有真实模型参与时，不声称验证了模型自主调度能力。

### 同一实验中的执行边界

复用[已有 Loop](../experiments/minimal-agent-ts/src/agent.ts)的轮数、超时和取消机制，以及[退款工作流](../experiments/refund-agent-real/src/workflow.ts)的阶段判断，先解释现有证据。

承接第三周的虚构草稿操作，用小型进程内演示观察以下边界：

| 场景 | 要观察的结果 |
| --- | --- |
| 尚未批准、批准、拒绝 | 未批准前不写入；批准来自测试驱动的用户输入，不能用模型自填字段代替；拒绝后没有草稿 |
| 相同操作重复提交 | 同一键、相同参数返回同一结果，只有一条记录；同一键配不同参数报告冲突 |
| 写入前取消 | 不再开始写入，运行记录为取消；另外解释写入后取消的局限 |
| 暂时失败与不可重试失败 | 确定性故障展示有界重试；非法参数和权限拒绝等先修正条件，不能盲目重试 |

学习者结合审批判断或重复提交判断中的一处修改讨论预期；助手实现并验证，学习者核对实际效果。案例共用一个小实验，不分别建设完整系统。

**完成证据：**异步任务完整事件记录、输入时机对照、状态图、边界结果和一次关键判断修改与学习者对其效果的解释（助手可代写）；能解释失败所在层次。数据库事务、并发竞争、Worker、崩溃恢复和跨进程幂等进入后端阶段；所选最小演示不记为这些能力已经实现。

## B. 第 7 章：评估与失败诊断

**需要掌握：**

- 将任务输入、初始状态、预期结果、过程约束与判定规则分开记录。
- 区分工具单元测试、Agent 任务评测和运行过程检查。
- 确定性检查、人工核对、LLM Judge 各适合判断什么；Judge 也需要标准与人工校准。
- 成功率、工具错误、步数、延迟、Token 和成本如何帮助定位问题；没有实际测量的数据记为未知。
- 样本如何覆盖正常、边界和失败，为什么少量案例通过不足以代表普遍可靠。

**最小实践：**将第三周已有的工具/Coding 检查与本单元 A 的正常、非法参数、拒绝、重复提交、取消和暂时失败案例整理成固定评测表。学习者讨论一条检查或一个已有检查遗漏的边界，由助手补代码并验证，不另建评测框架。

对照退款项目的[六个案例和验证器](../experiments/refund-agent-real/src/evaluation.ts)，选一个成功记录与一个失败记录，解释自动检查支持哪些结论、哪些仍需人工读证据。设计一条适合人工或 Judge 使用的简短评分标准，本单元不要求接入新的 Judge 模型。

**完成证据：**评测表、一次有原因的检查改动、一组成功/失败归因。复用真实 Trace，离线回放与新运行分别标记，不为收集指标而默认重跑全部付费接口。

## 带学与实现边界

先明确预期，再定向阅读与运行，最后根据证据解释。助手负责协议、演示、关键判断修改与检查代码；学习者参与预期讨论、亲跑、结果核对与解释，分别记录贡献；不要求学习者手写代码。

- 本单元的状态和重复提交实验限于进程内虚构数据；持久任务、数据库约束、Worker 与跨进程恢复进入后端阶段。
- 审批针对具体动作与参数；模型声称获批不能代替可信用户输入。
- 取消不保证撤销已经完成的效果；Trace 存在不代表可以自动恢复。
- 实际结果、离线回放、助手演示与学习者操作分开记录。
- 按上方区间预估投入，不设固定课时上限或总聊天请求次数。实际模型运行按验证缺口安排，保留单次运行限制；不因介绍 Judge 就默认调用新模型。
- 多模态系统实现、大规模评测平台、复杂并发与生产治理按需延后。

## 结束标准

| 验收项 | 足够的证据 | 当前状态 |
| --- | --- | --- |
| 异步 Agent 与运行控制 | 学习者亲跑“启动任务→中途用户事件→完成/取消→后续处理”，保存关联记录与时机对照；解释交互框架和状态图，核对执行边界，保留关键判断修改 | 未开始 |
| 系统评估与归因 | 评测表、检查改动与学习者解释、成功/失败分析和评分标准；说明自动检查与人工/Judge 的边界 | 未开始 |

两项满足即完成本单元。第三周通过项直接引用，不重做；不要求通读两章全部细节、实现生产恢复或新建评测框架。

## 阅读入口

- [原书第 6 章：交互——观察与动作空间的扩展](https://bojieli.github.io/ai-agent-book/book/chapter6/)：交互框架、异步任务与事件驱动；语音、GUI 与机器人作概览。
- [官方 `async-agent` 实验](https://github.com/bojieli/ai-agent-book/tree/main/chapter6/async-agent)：异步 Agent 主实践入口，选择最小路径，不要求全量框架复现。
- [原书第 7 章：Agent 的评估](https://bojieli.github.io/ai-agent-book/book/chapter7/)：任务与样本、确定性验证、Judge、轨迹和指标。
- [第三周计划](./week-03-plan.md)与[第二周复盘](./week-02-review.md)：已有能力和证据。

章节编号按当前在线原书。仓库 `06-evaluation` 等目录仍是旧编号，按主题与链接定位。

## 修订记录

- 2026-09-14：按用户选择拆为第 6、7 章学习单元，补齐 `async-agent` 的事件链路、运行入口与验收。
- 2026-09-14（用时补充）：用户认可第三、四周内容后，要求补充每块预计学习时间。本单元核心学习估为 9.5–14 小时，助手独立准备和额外排障另列；不设日期或固定课时上限，范围与验收保持不变。
- 2026-09-14（带学边界）：按用户要求补充“抓大放小”执行规则并关联 AGENTS.md，明确完整事件链亲跑、A3 共用实验且只亲改一处判断、复用评测证据和既有结束标准；未启动学习或实验。

- 2026-09-14（学习方式）：按用户最新偏好，将关键判断和检查代码交由助手实现；学习者参与行为与取舍讨论，取消手写门槛，保留异步 Agent 实际事件链与验证。本单元仍未开始。

## 执行进度

| 项目 | 当前记录 |
| --- | --- |
| 状态 | 分组计划已写，尚未开始 |
| 当前依据 | 2026-09-14 用户选择两章一组，并指出旧 `async-agent` 实践未明确保留；本次补齐异步事件链路、运行入口与验收 |
| 学习与验收 | 均未开始，0/2 |
| 实验与真实模型调用 | 本单元未实施、未调用 |
| 前置状态 | 是否满足前置以第三周执行进度为准；第四周不提前启动 |
| 下一步 | 完成第三周后，从本页 A 的异步 Agent 框架与最小运行入口进入 |

暂停和恢复时核对本页进度，记录实际证据、困难、调用情况与唯一下一步。时间由后续互动调整，记忆补课的历史预算和暂停状态保持独立。
