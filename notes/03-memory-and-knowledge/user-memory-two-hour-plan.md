---
title: 用户记忆最小闭环：两小时学习计划
chapter: 3
type: plan
status: paused
updated: 2026-09-13
---

# 用户记忆最小闭环：两小时学习计划

## 目标与时间边界

学会跟踪这一条完整链：从用户聊天中提取记忆 → 校验并保存 → 新对话读取并进入模型上下文 → 用户纠错 → 更新后再次验证。

计划内容 110 分钟，另留 10 分钟提问和故障缓冲，总上限 120 分钟。包含助手搭建、调试及带学时间，不把额外准备时间隐藏在课外。计划拟定时未执行；用户已明确要求开始，首次执行起点为 2026-09-13 21:24:03（Asia/Shanghai）。

这是用户主动选择的补充实践，不改变 Stage 2 已完成的状态，也不是进入 Stage 3 的强制前置要求。时间是正常环境下的估算，不保证接口故障时仍能完成所有真实验证；到上限如实记录完成项和缺口，不自动延长课程。

## 只做一个小场景

使用虚构用户，记住一个明确偏好：讲解代码时希望简短还是详细。

1. 用户甲说：“讲代码时请简短一点。”真实模型提取偏好候选，程序校验后写入本地 JSON。
2. 结束进程，开启新对话，仅提问“解释一下工具调用”。不复制上次聊天；程序读取用户甲的偏好并加入模型请求，观察回答。
3. 用户甲明确纠正：“改一下，以后讲代码请详细解释。”模型提取新候选，程序更新同一用户的同一个偏好字段，不把两个相反偏好同时当作当前事实。
4. 再开新进程，提出相同问题，核对当前记忆、实际模型输入以及回答变化。
5. 切换为虚构用户乙，检查其读取结果不含用户甲的偏好。

没有旧聊天历史却仍能使用持久化偏好，才体现这次实验中的跨会话记忆；同一会话一直带着历史重发不能替代此项验证。

## 110 分钟安排

| 时间段 | 用时 | 做什么 | 可见结果 |
| --- | ---: | --- | --- |
| 0–30 分钟 | 30 分钟 | 助手复用现有 TypeScript、模型适配与测试工具，搭建最小实验并做冒烟验证 | 可运行入口、少量本地测试、记忆文件和脱敏记录；学习者不承担环境排障 |
| 30–45 分钟 | 15 分钟 | 带着学习者完整看一遍准备阶段已取得的真实新增、跨会话读取、纠错和更新后读取记录，不重复调用接口 | 对照用户原话、候选记忆、保存内容、模型输入和回答 |
| 45–75 分钟 | 30 分钟 | 只读提取、校验保存、读取注入和更新四个关键入口 | 说清每一步输入、输出，区分模型建议与程序写入 |
| 75–95 分钟 | 20 分钟 | 学习者亲手换一条偏好表达，做一次显式纠错、新进程复验和用户隔离检查 | 新旧有效值对照；实际请求使用新值；另一用户读不到该记忆 |
| 95–110 分钟 | 15 分钟 | 用一次运行记录复述完整链，确认验收并整理简短笔记 | 一份五行总结及通过/未通过的证据 |
| 110–120 分钟 | 最多 10 分钟 | 仅用于必要提问或处理已出现的故障；无需要则提前结束 | 不新增知识点、工具或实验 |

## 实现范围：保持小而完整

- 在当前仓库内做独立的最小记忆实验，复用已有模型调用能力，不改动退款判断主线；实现仅在用户明确要求开始后进行。
- 模型负责从用户原话提取结构化候选；程序检查允许的字段和值，再决定写入或不操作。不是用写死答案或正则匹配冒充真实模型提取。
- 只支持“代码讲解详略”一个偏好字段，按用户和字段精确读取，不增加向量检索。
- 用户标识由程序的演示会话确定，不接受模型自行指定其他用户。此隔离只是本地教学边界，不代表生产身份认证。
- JSON 保存当前偏好及必要来源和更新时间；更新修改同一条有效记录，旧值可以留在实验日志中，但不再作为当前偏好注入。
- 读取发生在明确的代码讲解场景，仅注入对应用户的相关偏好，不把文件全部内容或原始聊天历史塞进请求。
- 保留脱敏的实际请求与响应用于观察，不保存密钥，不使用真实个人敏感信息。

计划只带读三类业务代码：提取与上下文组织、记忆存取与更新、命令入口。测试只看断言，不逐行讲辅助实现。

## 验收：四项即可，不再追加题目

1. **提取与保存**：用户原话产生了真实模型候选，程序校验后保存正确字段；不涉及偏好的输入可以不写入。
2. **跨会话使用**：新进程不带旧聊天，实际请求仍包含已保存偏好；人工检查回答是否遵循。读文件成功、请求包含记忆、模型正确使用记忆分别记录，不能互相代替。
3. **纠错与隔离**：更新后只读取当前新值；不同用户不会读到对方的偏好。隔离规则可以用本地测试验证，不额外要求另一轮真实聊天。
4. **学习复述**：学习者能结合一条记录解释“从哪里提取、谁决定写入、如何跨会话读取、纠错后改了什么、如何确认生效”。允许看代码，不要求背诵。

达到这四项即收尾；模型恰好给出长短不同的回答不是唯一证据，也不由单次结果推断长期稳定性。

## 控制时间与调用量

- 整个实验准备与跟学合计最多 9 次真实聊天接口调用，不使用 Embedding；这是次数保护，不是金额上限，也不保证全部请求成功。原安排为 8 次；2026-09-13 用户明确批准增加 1 次助手复验，总时间上限仍为 120 分钟。复用已确认的 `deepseek/deepseek-v4-flash-0731` 及本地凭据，不更换模型或修改配置。
- 分配为准备阶段 5 次（首次提取、新会话回答、纠错提取、更新后新会话回答及截断后的单次复验），学习者操作最多 4 次；已有 4 次记录保留，讲解复用真实记录，不重复跑一套。失败尝试也计入上限，其余程序边界用明确标注的离线测试检查。
- 接口报错保留记录，不无限重试，不自动换模型或增加预算。
- 排障最多使用 10 分钟缓冲；仍失败就明确区分“代码规则已验证”和“真实完整流程未验证”。离线替身不能充当真实运行成功。
- 时间紧时删减额外例子、逐行语法讲解和实现细节，不删跨会话、纠错与实际请求检查来宣称闭环完成。
- 未在 120 分钟内通过的部分单列为缺口，是否继续由学习者决定，不重新打开 Stage 2 的必修清单。

## 本次不学

向量数据库、复杂记忆检索、多层记忆架构、自动遗忘、通用冲突推理、并发写入与事务、生产部署，以及完整框架源码。只处理明确表达的偏好和明确纠错，不做隐含人格推断。

## 后续带学约束

已在仓库根目录 [AGENTS.md](../../AGENTS.md) 设置本课的项目级指引：继续前完整读取本计划，按原范围推进，每阶段核对并写回进度；不擅自加时、增加调用或追加结业要求。这不等于修改平台的系统提示词。

## 执行进度

本节是后续带学的接续记录，每阶段结束或暂停时更新；不能把计划安排写成已经完成的事实。时间与接口尝试跨会话累计。累计时间包含实际准备、调试和教学时段，明确休息或暂停不计入；不清楚的时长注明估算或待确认。

| 项目 | 当前记录 |
| --- | --- |
| 状态 | 学习者真实操作 4/4 全部通过；首次复述已提交，首尾两点需校正，不能标为全部掌握；在两小时时间边界暂停，不自动延长 |
| 当前阶段 | 第 5 阶段：已核对首次复述，程序保存、按用户和字段读取、更新同一记录三个主干正确；本轮给出提取来源与完整生效证据的针对性纠正，并记录笔记 |
| 累计执行时间 | 上个已计课堂时段 110 分 22 秒，加 23:21:40–23:30:28 的连续课堂时段 8 分 48 秒，合计 119 分 10 秒；另有此前 22:23:12–22:27:50 的 4 分 38 秒含计划讨论，切分待确认，累计上界为 123 分 48 秒。均为 Asia/Shanghai，连续时段含轮间阅读与操作等待，不代表个人专注时长；现已接近或达到 120 分钟边界，暂停后续教学，不以时间不确定为由自动加时；若有明确休息可核实后扣除 |
| 累计真实接口尝试 | 9 / 9 次，额度已全部使用：助手准备 5 次（含失败），学习者操作 4 次；不再发起真实模型调用，不重置记录或提高上限 |
| 已完成证据 | [全部真实记录](../../experiments/user-memory-minimal/README.md)；此前类型检查、13/13 离线测试及助手真实闭环通过；学习者 6–9 次分别 ADD brief、跨进程简短回答、[UPDATE detailed](../../experiments/user-memory-minimal/runs/2026-09-13T15-19-51-609Z-remember-79ffede7.json)、[更新后完整详细回答](../../experiments/user-memory-minimal/runs/2026-09-13T15-20-09-191Z-ask-19eccac9.json)；四个 PID 不同，最后请求只有规则、当前 detailed 与当前问题，回答三段、331 个汉字、finish_reason=stop；learner-a 仅一条当前记录，demo-a 未被改写，demo-b 只读为空 |
| 待验证 | 只剩原验收中的两点：新记忆从本次用户原话提取，不是从文件提取；确认完整生效需核对正确的新值、实际请求和模型回答，不能只看 action。纠正已提供，尚未再次确认学习者理解；不追加其他题目或实验 |
| 唯一下一步 | 询问是否愿意额外用约 2 分钟确认这两个原有要点；未经同意不继续计时教学、不增加调用。若同意，只补这两点并更新收尾状态，不重新讲整课 |
| 最近更新 | 2026-09-13 23:30:28 检查点；学习者提交首次复述，助手核对当前源码，保留用户新增注释；未改实现、未调用模型。已写回正确理解和待校正点，不把“给出纠正”冒充“学习者已掌握”；此前故障处理约 9 分钟缓冲已计入累计，原时间上限未调整 |

## 收尾复述记录与纠正

学习者首次复述已说清：检查和保存由程序完成；新进程按 userId 与 key 匹配旧记忆；用户纠错后更新同一记录，不另加一条矛盾偏好。

需要校正两点：

1. **提取与读取不同**：程序把本次用户原话和提取规则发给模型，由模型提出新候选；程序校验后保存 JSON。新对话从 JSON 读取的是已保存记忆，并须把它加入实际模型请求，文件不会被模型自动读取。JSON 是此实验中的持久化结果，不是首次提取的原始来源。
2. **action 不是完整生效证据**：当前实现返回 UPDATE 前已经 await saveJson，因此它说明程序完成了更新写入步骤；但仍需核对保存的新值是否正确、下一次实际请求是否只含当前新偏好、回答是否遵循。只有动作名称不能证明记忆在后续回答中完整生效。

真实技术流程已经全部通过。以上为助手针对首次复述提供的纠正，尚不等于学习者已重新正确复述；仅待这两点确认，不新增学习范围。

## 后续决定：通用记忆系统实验延后

2026-09-13，用户明确选择将“不预先写死业务字段的通用记忆教学原型”放到后面。预计额外 4–6 小时，包含搭建、真实验证与跟学；仅为估算，不是启动、加时或新接口预算授权。当前未开始、未预约、不自动排入 Stage 3；用户主动选择或出现实际项目需求时，再约定范围和预算。

本次仅同步学习记录，不恢复已暂停的入门实验教学；真实操作 4/4 通过、首次复述两点待确认和 9/9 次调用的状态不变。此决定已同步到[Notion：Stage 2 上下文、记忆与 RAG](https://app.notion.com/p/3c40d28175e7802aaf38db745a2d0d60)及仓库延后清单。

## 相关记录

- [Stage 2 复盘：用户记忆的已有基础](../stage-02-review.md#2-用户记忆)
- [延后学习清单](../learning-backlog.md)
- [第三章目录](./README.md)
